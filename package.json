{
  "dependencies": {
    "ethers": "^6.6.0",
    "fs": "^0.0.1-security",
    "solc": "0.8.7-fixed"
  },
  "scripts": {
    "compile": "yarn solcjs --bin --abi --include-path node_modules/ --s/ --base-path . -o . SimpleStorage.sol"
  }
}
